/**
 * Accent themes. Each one repaints only the accent tokens — the cream page,
 * white cards and shadow system stay the same across all of them.
 *
 * Shared because content names them, the picker renders them and the theme
 * hook persists them.
 */
export type ThemeId =
  | 'amber'
  | 'terracotta'
  | 'crimson'
  | 'forest'
  | 'indigo'
