/**
 * How the user rates their recall. Shared because the content file names these
 * keys, the grade buttons render them and the review session maps them to an
 * SM-2 recall quality.
 */
export type GradeKey = 'again' | 'hard' | 'good' | 'easy'
